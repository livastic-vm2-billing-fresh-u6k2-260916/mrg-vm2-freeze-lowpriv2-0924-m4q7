merge_protections_settings:
  reporting_method: check-runs
  post_comment: true

merge_protections:
  - name: VM2 retarget main-only blocker Q6N4
    if:
      - base = main
    success_conditions:
      - check-success = VM2_RETARGET_NEVER_GREEN_Q6N4
