scopes:
  source:
    files:
      freeze-final:
        include:
          - freezearea/**
      config:
        include:
          - .mergify.yml

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - label = VM2_MQ_FINALIZER_RELEASE_R6K3
    merge_method: merge

merge_queue:
  max_parallel_checks: 1
