scopes:
  source:
    files:
      freeze-final:
        include:
          - freezearea/**
      config:
        include:
          - .mergify.yml

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-neutral = Mergify Merge Protections
    merge_method: merge

merge_queue:
  max_parallel_checks: 1
